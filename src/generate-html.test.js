import {generateIndexHTML} from'./generate-html'

describe("generateIndexHTML", () => {

    it("test should return undefined", () => {
      expect(generateIndexHTML('')).toBe(undefined);
    });

  });

  